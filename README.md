# archive-youtube-backlink
Single-file tool that generates YouTube backlink URLs, then archives each one via Wayback + archive.today (random TLD) with visible IFRAME/Popup/Tab runners, concurrency, shuffle, share links, and downloadable backlink list. Vanilla JS, client-side only.
